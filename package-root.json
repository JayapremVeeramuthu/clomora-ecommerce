{
    "name": "teemaster-complete-system",
    "version": "1.0.0",
    "description": "Complete Teemaster Ecommerce System - Client + Admin + Backend",
    "scripts": {
        "install-all": "npm install && cd admin-panel && npm install && cd ../backend && npm install && cd ..",
        "dev": "concurrently \"npm run dev\" \"cd admin-panel && npm run dev\" \"cd backend && npm run dev\"",
        "dev:client": "npm run dev",
        "dev:admin": "cd admin-panel && npm run dev",
        "dev:backend": "cd backend && npm run dev"
    },
    "devDependencies": {
        "concurrently": "^8.2.2"
    }
}